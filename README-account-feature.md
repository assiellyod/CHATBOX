# Account button

The CHATBOX sidebar now includes an **Add account** button that opens `register.html`.

Current registration UI is frontend-only. Connect the form to the repository's authentication/API layer before using it for real accounts.
